============
Installation
============

At the command line::

    $ easy_install django-balystic

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-balystic
    $ pip install django-balystic
